import React from "react";
import Circle from "../reusable-elements/Circle";

const Circles = () => {

    return (
        <section className="icons">
            <Circle />
            <Circle />
            <Circle />
            <Circle />
        </section>
    );
}

export default Circles;